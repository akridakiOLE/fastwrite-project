venv/
*.pyc
__pycache__/
*.pdf
data/
exports/
processed/
uploads/
secrets/
GRAMMATEAS_Backup/
PYEOF
*.db
*.log

# PyInstaller build artifacts (Phase 4)
build/
dist/
*.spec.bak

# Master project doc — Google Drive only, NEVER στο git
FastWrite_Master_Project_*


# Demo video binaries — host on YouTube/Vimeo, never commit
*.mp4
*.mov

# Beta installer zip — host on GitHub Releases, never commit (~80MB)
FastWrite-Windows.zip

# Wrangler local cache — contains account info, NEVER commit (repo is public)
.wrangler/

# Benchmark artifacts (200-invoice PDF + preview PNGs) — local only, never commit
benchmark/

# Plaintext API keys for tests — NEVER commit (security)
new_keys.txt
aq_key.txt

# Temporary diagnostic/utility scripts — local only, not for commit
diag_*.py
benchmark_report_*.json
*_report_*.json
diag_rate.py
diag_multikey.py
diag_model.py
diag_lite.py
whichkey.py
add_key.py
del_extra_keys.py
testkeys.py
test_aq.py
